{
  "type": "module",
  "name": "seo-macak",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --ssr src/entry-server.jsx --outDir dist/server && node scripts/prerender.mjs && node scripts/generate-sitemap.mjs && node scripts/cleanup-ssr.mjs",
    "preview": "vite preview --port 5000"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "sharp": "^0.34.5",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "@emailjs/browser": "^4.4.1",
    "framer-motion": "^10.12.5",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-helmet-async": "^3.0.0",
    "react-router-dom": "^7.11.0"
  }
}
